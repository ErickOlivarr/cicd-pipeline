name: CI Pipeline2

on:
  push:
    branches: [ main ]

jobs:
  # TRABAJO 1: Se ejecuta MIENTRAS el PR está abierto
  build-push:
    runs-on: ubuntu-latest
    steps:
      - name: Cancel Previous Runs
        uses: styfle/cancel-workflow-action@0.9.1
        with:
          access_token: ${{ github.token }}
      - uses: actions/checkout@v4
      - name: Ejecutar docker image
        run: echo "se va a subir imagen a docker hub"
